name: suggest_a_feature
description: Suggest a Feature
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  bot_toast: ^4.0.1
  cached_network_image: ^3.2.0
  extended_image: ^6.3.1
  flutter_bloc: ^8.0.1
  flutter_svg: ^1.0.3
  http: ^0.13.4
  intl: ^0.17.0
  rxdart: ^0.27.3
  equatable: ^2.0.5

  sliding_sheet:
    git:
      url: https://gitlab+deploy-token-547341:NRrK4ohMCyQXqLmHgSwW@gitlab.com/sheep-apps/sliding_sheet.git
      ref: master

dev_dependencies:
  build_runner: ^2.3.0
  flutter_test:
    sdk: flutter
  mockito: ^5.3.2
  bloc_test: ^9.1.0

flutter:
  assets:
    - assets/
